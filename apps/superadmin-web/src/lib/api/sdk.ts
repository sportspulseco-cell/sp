// Thin re-export — implementation lives in @sportspulse/api-client.
// (Slice 5: SDK extracted to a shared package per repo owner ask
// "isn't SDK more efficient in the long run?".)
export * from "@sportspulse/api-client";
