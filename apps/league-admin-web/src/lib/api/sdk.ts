// Thin re-export — implementation lives in @sportspulse/api-client.
// Same convergence pattern as superadmin-web/lib/api/sdk.ts; eliminates
// the local copy that was drifting from the shared SDK.
export * from "@sportspulse/api-client";
