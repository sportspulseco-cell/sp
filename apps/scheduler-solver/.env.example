# Required in production. The orchestrator (Supabase Edge Function) must
# send this exact value in the `X-API-Key` header on every /solve call.
# On Hugging Face Spaces, set this as a Space Secret (Settings → Secrets).
SOLVER_API_KEY=replace-me-with-a-long-random-string
